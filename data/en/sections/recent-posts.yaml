# section information
section:
  name: Recent Posts
  id: recent-posts
  enable: true
  weight: 2 # order to display section inin (default: alphabetical followed by weight)
  showOnNavbar: true
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
